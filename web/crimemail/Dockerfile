# file automatically generated by mkctf.
FROM ubuntu

RUN apt-get update && \
    apt-get upgrade -y && \
    apt-get install -y apache2 php

RUN mkdir -p /var/www/html

COPY server-files/ /var/www/html

